spring:
  application:
    name: Violet-App-Admin
  cloud:
    nacos:
      config:
        server-addr: 39.100.41.190:8001
        group: DEFAULT_GROUP
        prefix: violet-app-admin
        file-extension: yaml
      discovery:
        server-addr: 39.100.41.190:8001

logging:
  level:
    org:
      springframework:
        boot:
          actuate:
            endpoint:
              EndpointId: error
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error

